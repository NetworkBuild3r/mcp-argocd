apiVersion: v1
kind: Service
metadata:
  name: mcp-argocd
  namespace: mcp-argocd
  labels:
    app.kubernetes.io/name: mcp-argocd
spec:
  type: ClusterIP
  selector:
    app: mcp-argocd
  ports:
    - name: http
      port: 3002
      targetPort: 3002
      protocol: TCP
